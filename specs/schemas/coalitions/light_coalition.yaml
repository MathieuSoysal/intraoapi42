LightCoalition:
  type: object
  properties:
    id:
      type: integer
    name:
      type: string
    slug:
      type: string
    image_url:
      type: string
      format: uri
    color:
      type: string
    score:
      type: integer
    user_id:
      type: integer
  required:
    - id
    - name
    - slug
    - image_url
    - color
    - score
    - user_id
