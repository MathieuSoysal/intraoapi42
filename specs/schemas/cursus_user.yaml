CursusUserResponse:
  type: object
  properties:
    id:
      type: integer
    begin_at:
      type: string
      format: date-time
    end_at:
      type: string
      format: date-time
    grade:
      type: string
    level:
      type: number
      format: double
    skills:
      type: array
      items:
        $ref: "#/components/schemas/SkillResponse"
    cursus_id:
      type: integer
    cursus:
      $ref: "#/components/schemas/CursusResponse"
    has_coalition:
      type: boolean
    blackholed_at:
      type: string
      format: date-time
      nullable: true
    created_at:
      type: string
      format: date-time
    updated_at:
      type: string
      format: date-time
  required:
    - id
    - begin_at
    - grade
    - level
    - cursus_id
    - cursus
    - has_coalition
    - created_at
    - updated_at
    - skills
