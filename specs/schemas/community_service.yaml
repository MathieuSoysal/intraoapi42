CommunityServiceResponse:
  type: object
  properties:
    id:
      type: integer
    duration:
      type: integer
    schedule_at:
      type: string
      format: date-time
    occupation:
      type: string
    state:
      type: string
    created_at:
      type: string
      format: date-time
    updated_at:
      type: string
  required:
    - id
    - duration
    - schedule_at
    - occupation
    - state
    - created_at
    - updated_a
