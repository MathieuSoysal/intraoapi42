Error:
  type: object
  properties:
    status:
      type: integer
      description: HTTP status code
    error:
      type: string
      description: Error message
  required:
    - status
    - error
  example:
    status: 401
    error: "The access token is invalid"
