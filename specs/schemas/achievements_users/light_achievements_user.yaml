LightAchievementsUser:
  type: object
  properties:
    id:
      type: integer
    user_id:
      type: integer
    login:
      type: string
    url:
      type: string
      format: uri
    created_at:
      type: string
      format: date-time
      x-go-type: IntraTime
  required:
    - id
    - user_id
    - login
    - url
    - created_at
