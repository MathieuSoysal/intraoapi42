CursusResponse:
  type: object
  properties:
    id:
      type: integer
    created_at:
      type: string
      format: date-time
    name:
      type: string
    slug:
      type: string
    kind:
      type: string
  required:
    - id
    - created_at
    - name
    - slug
    - kind
